# Configuração do Ruff (linter) para a API.
line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "B", "W"]
ignore = ["E501"]

[lint.per-file-ignores]
# main.py importa a camada de IA após a definição do app (documentado com noqa).
"app/main.py" = ["E402"]
